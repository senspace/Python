
stack.py
# 堆栈：堆栈是一个后进先出(LIFO)的数据结构，在栈上"push"元素就是把
# 一个对象添加到堆栈中，"pop"元素就是删除一个元素。

queue.py
# 队列：队列是一种先进先出(FIFO)的数据类型，新的元素通过“入队”的方式
# 添加进队列的末尾，“出队”就是从队列的头部删除。

# README:将列表做为堆栈用于存储和取回输入的字符串，这个菜单驱动程序
# 利用列表的append()和pop()方法模拟堆栈的"push"和"pop"操作。

